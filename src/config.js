export const PRODUCTION_EVIRONMENT = "production"; //else development
export const BASE_URL = "localhost:8000"; //need to put in port for multiplayer (none for prod)
export const API_URL = "localhost:8080";
export const DEFAULT_PORT = 8000;
